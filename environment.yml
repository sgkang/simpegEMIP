name: simpegEMIP-environment

channels:
  - conda-forge
  - defaults
dependencies:
  - numpy
  - scipy
  - matplotlib
  - ipywidgets
  - mkl
  - pip:
    - git+https://github.com/sgkang/simpegemip.git@master

